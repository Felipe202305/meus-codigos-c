#include <stdio.h>

int main() {
    int valores[7] = {12, 45, 7, 89, 23, 56, 4};
    int maior = valores[0];
    
    for (int i = i; i < 7; i++) {
        if (valores[i] > maior) {
            maior = valores[i];
        }
    }
    
    printf("maior valor: %d\n", maior);
    return 0;
}